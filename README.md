# C 语言练习代码

按日期整理的 C 语言学习练习，用 Visual Studio 编写。

- 每次练习一个目录（例如 `test_9.10`、`2026_3.10`），每个目录是一个独立的 VS 项目（`.sln` + `.vcxproj` + `.c` / `.cpp`）
- 覆盖 **114 个不同日期**，最早一次提交是 2025-07-25
- 编译产物（`.vs/`、`Debug/`、`Release/`、`x64/`、`*.ipch`、`*.pdb`、`*.obj`、`*.tlog` 等）不纳入版本管理，规则见 `.gitignore`

## 关于仓库历史

2026-09-16 对提交历史做了一次清理：从全部提交中移除了早期误提交的 Visual Studio 编译中间文件
（清理前工作区约 3.1 GB，其中约 3.08 GB 是编译产物）。

**所有原有提交的日期、提交信息、作者均未改动**，只是文件内容里不再包含编译产物。